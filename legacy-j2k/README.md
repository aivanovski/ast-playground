# legacy-j2k

Drop the old J2K implementation sources into this module.

Notes:
- Kotlin and Java sources are both picked up from `src/main/java` and `src/main/kotlin`.
- The module compiles against the local IntelliJ IDEA Kotlin plugin jars from `/Applications/IntelliJ IDEA.app/Contents`.
